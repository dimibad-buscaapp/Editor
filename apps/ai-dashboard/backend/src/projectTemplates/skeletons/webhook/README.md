# {{PROJECT_NAME}} — Webhook API

Fastify para receber webhooks com validacao de assinatura (header `x-webhook-signature`).

## Setup

1. Copie `.env.example` para `.env`
2. `npm install && npm run dev`

- Swagger: http://localhost:4000/docs
