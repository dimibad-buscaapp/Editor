# {{PROJECT_NAME}} — Express API

Express + Prisma + Swagger (swagger-jsdoc).

## Setup

1. `.env` com `DATABASE_URL`
2. `npm install && npm run prisma:migrate`
3. `npm run dev`

- Docs: http://localhost:4000/docs
- OpenAPI: http://localhost:4000/openapi.json
